name: "practice github workflow"

on:
  push:
    branches:
      - master
      - production
      - main

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
        run: echo "hi vinay"
