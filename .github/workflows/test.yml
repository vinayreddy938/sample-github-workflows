name: "multievent pipeline workflows"

on:
  push:
    branches:
      - master
      - production
      - main

  pull_request:
    branches:
      - main

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
        run: echo "hi vinay"

      - name: Print pull request
        run: echo "pull request triggered"
